export interface ServiceItem {
  id: number;
  title: string;
  description: string;
  icon: string;
}

export interface TestimonialItem {
  id: number;
  name: string;
  treatment: string;
  quote: string;
  image: string;
}

export interface NavItem {
  label: string;
  href: string;
}